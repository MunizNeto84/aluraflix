import styled from "styled-components";
const Form = styled.form`
  padding: 1.25rem 0;

  label {
    margin: 5px;
    align-self: flex-start;
    text-align: left;
    width: 100%;
  }

  button {
    margin: 10px;
  }
`;

export default Form;
